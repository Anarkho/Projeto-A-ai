//onde sera armazenada a combinação de todos os reducers

import { combineReducers } from 'redux'

const mainReducer = combineReducers({ //unindo as reducers em um som objeto

})

export default mainReducer